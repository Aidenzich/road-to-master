# Softmax 
softmax function = softargmax = normalized exponential function

## Formular
$$
\sigma(z)_i = \frac{e^{zi}}{\sum^K_{j=1}e^zj} \quad \text{for} \ i=1, ..., K \ and  \ z = (z_1, ..., z_k) \in \mathbb{R}_k
$$